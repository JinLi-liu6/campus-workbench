# 上线与开通步骤

## 一、GitHub Pages（前端网址，永久免费）

只需要你做一次，给我一个 token：

1. 打开 https://github.com/signup 用邮箱注册（没有账号的话）
2. 登录后点头像 → **Settings** → 左侧最下面 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. 点 **Generate new token (classic)**：
   - Note 随便写：`campus`
   - Expiration 选 **No expiration**（永不过期）
   - 勾选 **`repo`**（这一项就够）
4. 点 Generate，复制那串 `ghp_xxxxx` 发给我（只发给我，不要发群里；用完可以在 GitHub 上随时吊销）

我拿到 token 后：把代码推上去 → 开启 Pages → 给你网址，形如
`https://<你的用户名>.github.io/campus/`

以后我每次改版本，推一次就自动更新，网址永远不变。

---

## 二、云端同步后端（老师手机打卡 → 校长立刻看到）

二选一，两个都免费，脚本都写好了：

| 方案 | 速度 | 开通难度 | 说明 |
|---|---|---|---|
| 腾讯云开发 CloudBase | 最快（国内节点，实测 0.2 秒） | 微信扫码 + 实名 | 用 `后端/sync-func-tencent.js` |
| Cloudflare Worker | 较快（实测 0.75 秒） | 只要邮箱，不用实名 | 用 `后端/sync-worker-cloudflare.js` |

开通步骤写在对应脚本文件的顶部注释里。
拿到「接口地址 + 你自己设的通信密钥」后，填进工作台：
**设置 → 云端同步 → 接口地址 / 通信密钥 → 勾选开启自动同步 → 保存并连接**。

开启后：
- 任何一台设备打卡，1 秒内推到云端，其他设备每 30 秒自动拉一次（或手动点「立即拉取」）
- 出勤记录按「日期+场次+学生+老师」去重合并，取时间最新的那条 —— **不会互相覆盖**
- 断网也能用，数据先存本机，联网后补传

---

## 三、永久保底

无论网址怎么变，`index.html` 这一个文件在你自己电脑上，双击就能打开，不依赖任何服务商。
记得偶尔在「设置 → 导出数据」存一份 JSON 备份。
