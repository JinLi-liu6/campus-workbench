# 上线与开通步骤

## ✅ 已完成：前端已上线

- GitHub 仓库：https://github.com/JinLi-liu6/campus-workbench
- **主用网址：https://jinli-liu6.github.io/campus-workbench/**（GitHub Pages，永久免费）
- 备用网址：https://taolulisha-campus.surge.sh（内容一样，主站打不开时切这个）

### CDN 加速为什么放弃了（记录一下）

- jsDelivr：国内快且稳，但对 HTML 文件按纯文本返回（安全策略），浏览器显示源码不渲染 → 不能用
- statically / githack：能渲染，但国内访问被墙 → 不能用
- 结论：先用 github.io 原站。偶尔慢的话刷新一下，或临时切备用站。
- 彻底解决国内稳定的方案：开通 Cloudflare 或腾讯云（见下），我把前端也迁过去，一个账号、一个网址全搞定。

> ⚠️ 重要：浏览器的数据是按**网址（域名）**分开存的。**请固定用同一个网址**，不要今天用这个明天用那个，否则两边数据不通。如果已经在别的网址录了数据，用「设置 → 导出数据」导出 JSON，再到新网址「导入 JSON」搬过去。

### 更新版本的流程

我改完代码后推一次 GitHub，原站几分钟内自动更新；
jsDelivr 有缓存，我会帮你刷新（或你自己打开一次
`https://purge.jsdelivr.net/gh/JinLi-liu6/campus-workbench@main/index.html` 即可刷新）。

---

## 二、还没做：云端同步后端（老师手机打卡 → 校长立刻看到）

## 二、真实课表已导入（2026 秋季）

已从 `工作簿1.xlsx` 解析完成：**611 名学生、1090 条课程记录、22 位老师**。

| 项 | 数量 | 是否计入科目数 |
|---|---|---|
| 在读 | 935 | ✅ 计入 |
| 停课 | 122 | ❌ 不计入（单独显示） |
| 退费 | 33 | ❌ 不计入（单独显示） |

- 按科目：语文 170 / 数学 393 / 英语 372
- 按年级：七年级 270 / 八年级 275 / 九年级 390
- 学几科：1 科 206 人、2 科 260 人、3 科 56 人（在读共 522 人）
- 陶老师名下：63 科

**怎么用**：
- 本机：直接双击 `工作台-本地含课表.html`（已内置，不用导入）
- 网上版：设置 → 导入 JSON 备份 → 选 `课表数据.json`

**老师账号已建好**，密码统一 123456。表里有两位「李老师」，已区分成 **李老师**（三楼23号）和 **李老师2**（4楼2号）。

**还缺的信息**：课表里没有星期和上课时段，所以
1. 正课出勤目前是「列出该老师名下全部在读学生」，谁来了点谁；
2. 晚自习排班还是空的 —— 把「哪天哪个班上什么晚自习、几点到几点、值班老师」发我，我填进去，就能自动带出应到名单。

> 隐私说明：学生姓名等真实数据**只存在你本机**（`课表数据.json`、`工作台-本地含课表.html`），
> 已加入 .gitignore，不会上传 GitHub —— 那个仓库是公开的。

---

## 三、还没做：云端同步后端（老师手机打卡 → 校长立刻看到）

现在老师打卡的数据只存在自己手机上。要真正联动，二选一，两个都免费，脚本都写好了：

| 方案 | 速度 | 开通难度 | 脚本 |
|---|---|---|---|
| 腾讯云开发 CloudBase | 最快（国内节点，实测 0.2 秒） | 微信扫码 + 实名 | `后端/sync-func-tencent.js` |
| Cloudflare Worker | 较快（实测 0.75 秒） | 只要邮箱，不用实名 | `后端/sync-worker-cloudflare.js` |

开通步骤写在对应脚本文件的顶部注释里。
拿到「接口地址 + 你自己设的通信密钥」后告诉我，我填进去就能实时联动。

开启后：
- 任何一台设备打卡，1 秒内推云端，其他设备每 30 秒自动拉一次（也可手动「立即拉取」）
- 出勤记录按「日期+场次+学生+老师」去重合并、取时间最新的那条 —— **不会互相覆盖**
- 断网也能正常打卡，联网后自动补传

---

## 三、永久保底

不管网址怎么变，`index.html` 这一个文件在你自己电脑上，双击就能打开，不依赖任何服务商。
记得偶尔在「设置 → 导出数据」存一份 JSON 备份。

---

## 附：账号安全

你给我的 GitHub token 现在明文存在 `AI工作台/.git/config` 里（为了以后能随时帮你更新版本）。
如果哪天不想留了，去 GitHub → Settings → Developer settings → Personal access tokens → 点 **Delete** 吊销即可，
之后需要更新版本时再给一个新的就行。
